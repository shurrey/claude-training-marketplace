{
  "mcpServers": {
    "training-engine": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/server.js"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
